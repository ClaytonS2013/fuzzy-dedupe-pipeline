services:
  fuzzy-dedupe:
    # ... existing config ...
    
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
      - ENABLE_AI_FEATURES=true  # ADD THIS
      - AI_MODEL_CACHE=/app/models  # ADD THIS
      - TRANSFORMERS_CACHE=/app/models  # ADD THIS
